---
name: ds-work-resume
description: Alias for /ds-work-continue — resume work on a project.
---

This command is a hard alias for `/ds-work-continue`. Execute the full procedure defined in `commands/ds-work-continue.md`, passing through `$ARGUMENTS` as the target directory.
